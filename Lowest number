include<stdio.h>
main()
{
	int n,ele,l,i=1;
	printf("enter number of elements\n");
	scanf("%d",&n);
	while(i<=n)
	{
		scanf("%d",&ele);
		if(i==1)l=ele;
		if(ele<l)
		l=ele;
		i++;
	}
	printf("lowest=%d",l);
}
