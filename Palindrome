#include<stdio.h>
main()
{
	int pal,n,rem,sum=0;
	printf("enter a number\n");
	scanf("%d",&n);
	pal=n;
	while(n!=0)
	{
		rem=n%10;
		sum=(sum*10)+rem;
		n=n/10;
    }
    if(sum==pal)
    printf("Palindrome");
    else
    printf("Not a Palindrome");
}
