#include<stdio.h>
main()
{
	int n,rem,sum,arm;
	sum=0;
	printf("enter a number\n");
	scanf("%d",&n);
	arm=n;
	while(n!=0)
	{
		rem=n%10;
		sum=sum+(rem*rem*rem);
		n=n/10;
	}
	if(arm==sum)
	printf("Armstrong number");
	else
	printf("Not armstrong");
}
